{
  "name": "shopsphere",
  "private": true,
  "version": "1.0.0",
  "description": "ShopSphere — Multi-vendor e-commerce marketplace (MERN stack)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "ensure-accounts": "npm run ensure-accounts --prefix backend"
  }
}
